# Legacy code

This folder contains the previous Tkinter application entry point.

The current application entry point is the PySide6/Qt package under:

    src/airt

The legacy code is kept temporarily as reference while the processing pipeline is migrated into the new architecture.
