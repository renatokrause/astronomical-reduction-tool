# Legacy Tkinter application

This folder contains the previous Tkinter implementation and the previous reduction_tool package.

The current application entry point is:

    python -m airt

Current source code lives under:

    src/airt

This legacy code is kept temporarily as reference while the processing pipeline is migrated into the new architecture.
